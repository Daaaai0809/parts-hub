post:
  operationId: login
  summary: ログイン
  description: ログイン
  tags: 
    - Auth
  parameters:
    - in: body
      name: body
      required: true
      schema:
        $ref: '../../../schema/request/client/auth/login_request.yaml#/LoginRequest'
  responses:
    '200':
      description: ログイン成功
      schema:
        $ref: '../../../swagger.yaml#/definitions/EmptyResponse'
    'default':
      description: エラー
      schema:
        $ref: '../../../swagger.yaml#/definitions/ErrorResponse'